# Gemini API Configuration
GEMINI_API_KEY=your_api_key_here

# Optional: Change the Gemini model (default: gemini-2.5-flash)
# GEMINI_MODEL=gemini-2.5-flash
# 
# For longer responses without truncation, consider using:
# GEMINI_MODEL=gemini-1.5-pro
# GEMINI_MODEL=gemini-1.5-flash
#
# Note: gemini-2.5-flash has an 8K output token limit
# gemini-1.5-pro has higher limits but may be slower/more expensive

# Optional: Change the folder where Telos files are stored (default: telos)
# TELOS_FOLDER=telos
